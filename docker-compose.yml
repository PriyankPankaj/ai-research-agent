services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./sessions.db:/app/sessions.db
      - ./chroma_db:/app/chroma_db
    environment:
      - HF_HUB_OFFLINE=0
    restart: unless-stopped